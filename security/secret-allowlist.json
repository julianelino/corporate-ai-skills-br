{
  "note": "Exact matched strings, not patterns. Used only for deliberately fake fixture secrets that would otherwise trip scripts/scan-secrets.mjs. Adding an entry here is a review action, not a way to silence a real finding — never add a real credential's value here, allowlist the fixture string instead of removing the check.",
  "allowed": []
}
