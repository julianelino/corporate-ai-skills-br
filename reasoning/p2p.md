# Procure-to-Pay Reasoning

Follow `document → supplier → CNPJ → PO/contract → receipt → amount → tax → withholding → duplicate → due date → approval → payment → reconciliation`. An invoice or boleto is evidence, not an automatic authorization.
