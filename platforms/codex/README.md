# Codex

Supported repository-oriented structure. Discover `SKILL.md` files and load referenced material selectively; consume companion `skill.yaml` contracts for governance. No Python runtime is needed to consume the pack; reference engines are optional maintainer tooling.
