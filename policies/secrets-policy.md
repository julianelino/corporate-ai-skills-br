# Secrets Policy

Never store, print, or commit passwords, API keys, access tokens, private keys, banking credentials, or production connection strings. Use placeholders such as `${PAYMENT_API_TOKEN}` in examples. Stop and notify the owner if a suspected secret appears in repository content.
